# Database — MySQL
DB_USERNAME=root
DB_PASSWORD=your_mysql_password

# Stripe
# Set STRIPE_MOCK=true to use simulated payments (no real Stripe account needed)
STRIPE_MOCK=true
# For real Stripe test payments: get your key from dashboard.stripe.com
STRIPE_SECRET_KEY=sk_test_your_stripe_test_secret_key
